# راهنمای سریع وانا فارسی

## 🚀 شروع سریع

### 1. نصب خودکار (توصیه شده)

```bash
# کلون کردن پروژه
git clone <repository-url>
cd vanna-farsi-local

# اجرای نصب خودکار
python setup.py
```

### 2. اجرای برنامه

```bash
# راه‌اندازی Ollama
ollama serve

# در ترمینال جدید، اجرای برنامه
python app.py
```

### 3. استفاده

1. مرورگر را باز کنید
2. به آدرس `http://localhost:5000` بروید
3. سوال خود را به فارسی بنویسید
4. نتایج را مشاهده کنید

## 📝 مثال‌های سوال

- "نمایش تمام مشتریان"
- "۱۰ مشتری برتر"
- "میانگین حقوق کارمندان"
- "فروش کل محصولات"

## 🔧 عیب‌یابی سریع

### مشکل: Ollama کار نمی‌کند
```bash
ollama serve
```

### مشکل: مدل دانلود نشده
```bash
ollama pull llama2
```

### مشکل: پورت اشغال است
```bash
# تغییر پورت در فایل .env
PORT=5001
```

## 📚 مستندات کامل

برای اطلاعات بیشتر، فایل `docs/README.md` را مطالعه کنید.

## 🆘 پشتیبانی

در صورت بروز مشکل، issues در GitHub ارسال کنید.